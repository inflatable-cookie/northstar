# Operators

This folder is for humans running Northstar in real work.

Start with [operator-quick-start.md](/Users/betterthanclay/Dev/projects/northstar/bundle-docs/operators/operator-quick-start.md).

Everything else here is support:

- `strict-planning-starter-prompt.md` for reusable planning thread openers
- `project-refocus-starter-prompt.md` for reusable recovery thread openers
- `live-project-refocus-specimen.md` for a worked recovery example
- `operator-workflow-drill.md` to test the stack
- `operator-pilot-record-template.md` to capture pilot evidence
- `operator-doc-pruning-rubric.md` to trim the doc set after use

## Next task

Keep this folder short and obvious: one front door, a small number of support
docs, and no duplicate routing pages.
