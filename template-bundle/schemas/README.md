# Schemas

Use this folder for formal schemas (JSON schema, payload schema, shape contracts).

## Rule

Schema updates should be traceable to roadmap and report entries.

## Next task

Add schema files only when structure validation is part of active roadmap scope.
