# Docs

## Start Here

- `vision/README.md`
- `architecture/README.md`
- `contracts/README.md`
- `roadmaps/README.md`
- `logs/README.md`

If the repo uses the stricter delivery layer, also start from:

- `contracts/001-working-rules.md`
- `specs/README.md`

## Working Rule

The baseline docs spine is `vision/`, `architecture/`, `contracts/`,
`roadmaps/`, and `logs/`.
For stricter repos, add `contracts/001-working-rules.md` and `specs/` so
execution grammar and batch-card planning are explicit.

Start in `vision/` for long-horizon direction, `architecture/` and `contracts/`
for the canonical structure and rules, `roadmaps/` for active execution
sequencing, and `logs/` for batch evidence and decisions.

If `specs/` exists, treat it as provisional planning that should promote into
`architecture/` and `contracts/` before execution relies on it.

Keep the live `Next Task` pointer in the roadmap front doors:
`roadmaps/README.md`, `roadmaps/generation-index.md`, and the active
`roadmaps/gNN/README.md`.
The active `roadmaps/gNN/README.md` also owns the generation runway so new
milestones come from generation intent rather than ad-hoc post-closeout
planning.
