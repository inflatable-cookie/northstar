interface:
  display_name: "Northstar Recover"
  short_description: "Recover changed or drifting plans."
  default_prompt: "Use $northstar-recover for Northstar recovery work: bounded replanning after change, broad refocus after drift, or sweep-led audit and repair, while restoring the active spec lane, removing stale shadow authority, and re-establishing the canonical promoted refs."
