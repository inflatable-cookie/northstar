interface:
  display_name: "Northstar Setup"
  short_description: "Set up repo docs and workflows."
  default_prompt: "Use $northstar-setup to scaffold or normalize this repo around Northstar docs, workflow conventions, and Effigy-backed validation. Classify the repo posture first, choose the correct authority mode and adoption mode deliberately, and install the stricter delivery-layer surfaces and execution guardrails when the project warrants them."
