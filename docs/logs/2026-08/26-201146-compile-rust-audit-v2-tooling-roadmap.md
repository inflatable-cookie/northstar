# Compile Rust Audit V2 Tooling Roadmap

Date: 2026-08-26
Roadmap: `g02.032`
Card: `g02.032/094`

## Outcome

Compiled the promoted Convergence live-use boundary into one active master spec,
one six-card milestone, and one ready boundary-proof card. The lane moves scope,
coverage completeness, tool evidence, limitations, and finalization into
deterministic tasks while retaining agent judgment for semantic review.

The TypeScript comparison selected two implementation precedents. Northstar's
TypeScript recorder supplies structured evidence and failure-stage semantics.
`dmmulroy/anti-slop` supplies the vendored, fixture-tested AST-rule and
source-to-skill parity model. Rust v2 adopts those mechanics only for precise,
qualified detectors; it does not import the external rule opinions or turn
heuristics into repair authority.

## State

- Spec 033 is active.
- Roadmap `g02.032` is active.
- Card 094 is ready and is the only card inside the continuation envelope.
- Cards 095-099 are visible but pending their preceding evidence gates.
- The distributed Rust v1 skill remains unchanged.

## Next task

Execute card 094 and freeze the portable command, schema, lifecycle, failure,
and context-budget boundary before editing the production payload.

