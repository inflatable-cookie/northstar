# Promote TypeScript Audit Catalogue

Date: 2026-08-26
Roadmap: `g02.031`
Card state: `g02.031/090` complete; `g02.031/091` ready

## Outcome

The production skill now owns one strict explicit-audit TypeScript/Svelte
catalogue, an exact audit projection, catalogue and consumer-profile schemas,
copy-ready profile/deviation templates, and a deterministic source check.

The catalogue contains nine approved normative rules and one experimental
evaluation signal. All normative repairs remain `review_required` by default.
Public error, package, request, and serialization contract changes remain
operator decisions. `TS-SLOP-001` remains non-failing and report-only.

Everyday guidance, ordinary/high-assurance profiles, and the deferred toolchain
and testing rules are absent and rejected. No router, mode, command, setup,
recorder, installed selector, or distribution surface changed.

## Evidence

- this closeout records the rule inventory, provenance boundary, hashes, seven
  negative paths, and claim limits;
- `effigy check:typescript-quality` passes catalogue, projection, schemas, and
  templates.
- `effigy qa:docs`, `effigy qa`, and `git diff --check` are the batch closeout
  board.

## Next task

Execute `g02.031/091`. Add the explicit-only route, setup, recorder, adapter,
and focused lifecycle tests. Do not claim production validity or distribution.
