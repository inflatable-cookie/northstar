# Promote And Distribute Rust V2

Date: 2026-08-27
Roadmap: `g02.032`
Card: `g02.032/099`
Status: complete

## Result

Promoted revision E unchanged into the configured global Northstar skill. The
source and `/Users/tom/.agents/skills/northstar` match across 120 files. Rust v2
remains two routed tracks inside one skill: compact everyday authoring and
explicit-only audit-and-repair.

The configured global entry still reports source `inflatable-cookie/northstar`
and includes Codex among its agents. This closeout used the documented local
development sync. It did not publish a release or mutate a consumer repository.

## Frozen evidence

- Cargo-native engine payload:
  `dab44a149ee02bc17777e575f42b8857685031a2e0ee21cbc66a981624ff471f`;
- Rust catalogue:
  `11cb55a8912795b8396791ae0645005a879549dcc4b3777cc998595fec44743f`;
- strict authoring projection:
  `8b8f8d0cad4e787f4eae0989bfbbfc8bcf306e07633b44be5813d4570c8cdd09`;
- strict audit projection:
  `5394f18fc7f09fb20356edb92a079fc706d912109b172e04d3ccef559120cc77`;
- detector ledger:
  `211edddd98741aeb803d2b44c7d2f5bb09ffb3e4ba55883b6ff6f57db26c5521`;
- complete 120-file skill payload:
  `5fae235aafd890a22d77ae51b96896b4855a41ed24e5adb517450a3c07795dea`;
- production cohort: exact `4/4` primary findings, `18/18` rule verdicts,
  `9/9` attestations, `3/3` accepted blind reviews, and no concerns.

## Installed surface

Nine thin command adapters remain. Nine installed Effigy tasks cover the
agent-instruction check; Rust package and activation checks; and TypeScript
package, setup, recorder, and self-tests. Rust recording is now owned only by
the Cargo-native engine; the superseded Rhai recorder and its task are absent.

The main skill, router, and everyday Rust mode remain 9,227, 12,726, and 4,571
bytes. Distribution added no always-loaded rule dump and did not make explicit
audit implicit.

## Validation

- source and installed skill-creator validation: pass;
- source/install parity: pass, 120 files and matching aggregate hash;
- embedded/source Cargo-native payload verification: pass;
- installed Rust package and setup self-tests: pass;
- installed TypeScript package, setup, and recorder self-tests: pass;
- nine installed tasks and nine command adapters inspected;
- full source package, command, bundle, docs, posture, agent-instruction, QA,
  and diff checks: pass; the instruction check retains its advisory findings.

Effigy Doctor remains non-green on the repository's pre-existing oversized-file
scan (`5` errors, `13` warnings) and a stale optional graph index. Neither is a
card-099 gate or was used as distribution evidence.

## Boundary

Supported: strict everyday Rust authoring and explicit worktree or repository
audit with anchor scope, complete ledgers, structured evidence, and
authority-bounded repair.

Unavailable: ordinary or high-assurance profiles, automatic unsafe/FFI repair,
blanket lint fixing, semantic architecture detectors, guaranteed scanner
services, observable compaction assurance, certification, NASA compliance, a
safety case, release mutation, or Northstar-dispatched consumer audits.

## Lane state

Card 099 closes `g02.032`. Continuation envelope and lane budget are exhausted
with pause signal `lane-complete`.

## Next

Accept operator-provided live-use feedback. Northstar does not dispatch a
consumer audit.
